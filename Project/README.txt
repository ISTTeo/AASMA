This project is dependant on the following libraries:
pandas, matplotlib, numpy, scipy, gym, gym-anytrading

As such, installation of these libraries is necessary prior to running the code.
All of these libraries are available through pip.

To run:
- Open the terminal in the current directory
- Insert the command './executable.sh' and press enter